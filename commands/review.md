---
description: Open the local session viewer and triage recorded runs.
argument-hint: "[session id — blank = latest / index]"
---

Delegate to the `review-agent` subagent. Session: **$ARGUMENTS**.

Ask it to list or summarize the relevant session(s), cite `report.html` paths, and run `luma-browser viewer [--session <id>] [--open]`.
